# SQL 通用语法笔记

## 基础说明

- SQL 整体不区分大小写，书写规范建议关键字大写。
- 多行注释写法和 C 语言多行注释一致。

## 通用语法规则

- SQL 语句可单行、多行书写，必须以分号 `;` 结尾。
- 语句中可使用空格、缩进排版，提升可读性。
- MySQL 的 SQL 不区分大小写，行业规范：关键字建议大写。

## 注释写法

### 单行注释

```sql
-- 注释内容
```

通用 SQL 单行注释。

```sql
# 注释内容
```

MySQL 特有单行注释。

### 多行注释

```sql
/* 注释内容 */
```
